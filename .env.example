# Plex Compress — Environment Configuration
# Copy to .env and customize for your host.

# ------------------------------------------------------------------------------
# Paths
# ------------------------------------------------------------------------------
PLEX_ROOT=/mnt/truenas-media/Video
CONFIG_DIR=~/.plex_compress
TEMP_DIR=~/tmp/plex_compress

# ------------------------------------------------------------------------------
# Encoding
# ------------------------------------------------------------------------------
# 'auto' detects NVIDIA > VideoToolbox (macOS native) > libx265 fallback
ENCODER=auto
QUALITY=28
PRESET=p4
PARALLEL=2

# ------------------------------------------------------------------------------
# Safety / Behavior
# ------------------------------------------------------------------------------
BACKUP=1
DRY_RUN=0
VERBOSE=1
VERIFY_CHECKSUM=0

# ------------------------------------------------------------------------------
# Container
# ------------------------------------------------------------------------------
IMAGE=plex-compress:latest
# CONTAINER_TOOL=docker    # or 'podman'
